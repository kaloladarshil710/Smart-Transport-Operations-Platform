<?php
/** Controlled driver availability update page. */
declare(strict_types=1);
require_once __DIR__.'/../../config/config.php';require_once ROOT_PATH.'/functions/driver_functions.php';requireAuth();enforceModuleAccess('drivers');$id=(int)($_GET['id']??$_POST['id']??0);$driver=getDriverById($id);if(!$driver)redirect('modules/drivers/index.php');if($_SERVER['REQUEST_METHOD']==='POST'){if(verifyCsrfToken((string)($_POST['csrf_token']??''))){[$ok,$message]=updateDriverStatus($id,(string)($_POST['status']??''));setFlash($ok?'success':'danger',$message);}redirect('modules/drivers/view.php?id='.$id);}$pageTitle='Driver availability';require ROOT_PATH.'/includes/header.php';require ROOT_PATH.'/includes/sidebar.php';
?><main class="main-content driver-module"><?php require ROOT_PATH.'/includes/navbar.php';?><section class="auth-shell"><form class="auth-card card" method="post"><input type="hidden" name="csrf_token" value="<?=e(csrfToken())?>"><h2>Update availability</h2><p><?=e($driver['full_name'])?> is currently <?=e(driverAvailability($driver))?>.</p><div class="form-group"><label>Status</label><select name="status"><?php foreach(['Available','Off Duty','Suspended','Inactive'] as $status):?><option<?=$driver['status']===$status?' selected':''?>><?=$status?></option><?php endforeach;?></select></div><button class="btn btn-primary">Update status</button></form></section></main><?php require ROOT_PATH.'/includes/footer.php';?>
