<?php
declare(strict_types=1);
require_once __DIR__.'/../../config/config.php';require_once ROOT_PATH.'/functions/vehicle_functions.php';requireAuth();enforceModuleAccess('vehicles');$id=(int)($_GET['id']??$_POST['id']??0);$vehicle=getVehicleById($id);if(!$vehicle){setFlash('danger','Vehicle not found.');redirect('modules/vehicles/index.php');}
if($_SERVER['REQUEST_METHOD']==='POST'){if(!verifyCsrfToken((string)($_POST['csrf_token']??''))){setFlash('danger','Your form session expired.');}else{try{deleteVehicle($id);setFlash('success','Vehicle archived successfully.');redirect('modules/vehicles/index.php');}catch(Throwable $e){setFlash('danger',$e->getMessage());}}redirect('modules/vehicles/view.php?id='.$id);}$pageTitle='Archive vehicle';require ROOT_PATH.'/includes/header.php';
?><main class="auth-shell"><form class="auth-card card" method="post"><input type="hidden" name="csrf_token" value="<?=e(csrfToken())?>"><h2>Archive vehicle?</h2><p><?=e($vehicle['registration_number'])?> will no longer be available for future dispatches. Active assignments must be completed first.</p><button class="btn btn-danger">Archive vehicle</button><a class="auth-link" href="view.php?id=<?=$id?>">Cancel</a></form></main><?php require ROOT_PATH.'/includes/footer.php';?>
