<?php /** Expense lookup endpoint. */ declare(strict_types=1);require_once __DIR__.'/../config/config.php';requireAuth();enforceModuleAccess('expenses');$q='%'.trim((string)($_GET['q']??'')).'%';$s=getDb()->prepare('SELECT id,title,amount,expense_date FROM expenses WHERE title LIKE ? OR vendor_name LIKE ? LIMIT 20');$s->execute([$q,$q]);jsonResponse(['ok'=>true,'rows'=>$s->fetchAll()]);
